{
  "name": "fantasticar-combo-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Visualiseur de decklist Duel Commander + simulateur Monte Carlo du combo The Fantasticar.",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "calibrate": "vitest run --reporter=basic test/calibrate.test.ts"
  },
  "dependencies": {
    "lz-string": "^1.5.0",
    "nuxt": "~3.14.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
